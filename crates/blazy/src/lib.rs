//! blazy — a Blender-style UI layer on top of Masonry.
//!
//! See `rnd/architecture.md` for the design this crate implements.
//! At this stage only [`canvas`] exists; it is the subject of the Phase 0
//! feasibility experiment (`examples/node-canvas`).

pub use blazy_canvas as canvas;
